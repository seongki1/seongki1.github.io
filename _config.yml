title: 경희무교로한의원
email: seongki1@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  시청 한의원, 통증, 디스크, 근막통증증후군, 체질개선 다이어트, 보약

avatar: /assets/img/logo122.png

baseurl: "" # the subpath of your site, e.g. /blog
url: http://mgrdoc.com

disqus: mgrdoc

twitter_username: seongki1
github_username:  seongki1

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
google_analytics:

collections:
  clinics:
    output: true
    # permalink: /clinics/:path

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
